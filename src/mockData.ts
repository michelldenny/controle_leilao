import {
  Auction,
  Lot,
  AuctionItem,
  AdditionalExpense,
  MaintenanceRecord,
  Advertisement,
  SaleRecord,
  Contact,
  AppDocument,
  ActivityLog,
  AlertItem,
} from "./types";

export const initialAuctions: Auction[] = [];
export const initialLots: Lot[] = [];
export const initialItems: AuctionItem[] = [];
export const initialExpenses: AdditionalExpense[] = [];
export const initialMaintenanceRecords: MaintenanceRecord[] = [];
export const initialAdvertisements: Advertisement[] = [];
export const initialSales: SaleRecord[] = [];
export const initialContacts: Contact[] = [];
export const initialDocuments: AppDocument[] = [];
export const initialActivityLogs: ActivityLog[] = [];
export const initialAlerts: AlertItem[] = [];
